/* Frontend API configuration. Update API_BASE_URL once the backend is deployed
   (see /server/README.md). Left pointing at localhost for local development. */
var CONFIG = {
  API_BASE_URL: 'http://localhost:5000'
};
